#include<stdio.h>

void main()
{
    // iterar sobre 1000 numeros (0-1000)
    // no intervalo encontrar numeros pares e contar quantos existe
    // contar quantos numeros primos existe e indicar quais são
}